mixin side-menu-list(list)
  .side-menu__list
    each tab in list
      - const { id, name, iconClasses, isChecked } = tab;

      label.file(
        class=isChecked ? 'file_active' : '',
        data-tab-id=`tab-${id}`,
        data-tab-action='open',
        for=`tab-${id}`
      )
        span
          i.file__icon(class=iconClasses)
          |
          | #{ name }
